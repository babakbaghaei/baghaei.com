# Frontend - Baghaei Tech Group

این بخش شامل کدهای سمت کاربر (Client-Side) وب‌سایت گروه فناوری بقایی است که با **Next.js 16** توسعه یافته است.

## 🚀 ویژگی‌های فنی

- **موتور توسعه:** استفاده از **Turbopack** (`next dev --turbo`) برای سرعت فوق‌العاده در زمان توسعه.
- **استایل‌دهی:** **Tailwind CSS v4** با سیستم تمینگ پویا (Light/Dark).
- **پرفورمنس:** بهینه‌سازی کامل فونت‌ها (`next/font/local`) و تصاویر (`next/image`).
- **امنیت:** میدل‌ورهای امنیتی، CSP و محافظت در برابر XSS.
- **تست:** تست‌های E2E با **Playwright**.

## 🛠 دستورات کلیدی

### راه‌اندازی سرور توسعه
```bash
npm run dev
# اجرا با Turbopack روی پورت 3000
```

### بیلد برای پروداکشن
```bash
npm run build
# بیلد بهینه شده با Webpack
```

### اجرای تست‌ها
```bash
npx playwright test
# اجرای تست‌های E2E
```

### بررسی کیفیت کد
```bash
npm run lint
# اجرای ESLint
```

## 📂 ساختار فایل‌ها

- `src/app`: صفحات و روت‌های اصلی (App Router).
- `src/components`: کامپوننت‌های بازاستفاده‌پذیر (UI, Layout, ...).
- `src/lib`: توابع کمکی، هوک‌ها و تنظیمات API.
- `public/fonts`: فونت‌های محلی بهینه‌سازی شده.