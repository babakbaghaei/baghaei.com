# Backend - Baghaei Tech Group

این بخش شامل کدهای سمت سرور و APIهای وب‌سایت است که با **NestJS** و **Prisma** پیاده‌سازی شده است.

## 🛡️ ویژگی‌های امنیتی و معماری

- **معماری:** ماژولار و مقیاس‌پذیر (Clean Architecture).
- **دیتابیس:** PostgreSQL با دسترسی Type-Safe از طریق Prisma.
- **امنیت:**
  - **Sanitization:** پاکسازی تمام ورودی‌ها برای جلوگیری از تزریق کد.
  - **Rate Limiting:** محافظت در برابر حملات DDoS.
  - **Auth:** احراز هویت با JWT و Hash کردن رمزها با Bcrypt.
- **مانیتورینگ:** لاگ‌های ساختاریافته (JSON) و اندپوینت Health Check.
- **مستندات:** Swagger UI خودکار.

## 🛠 دستورات کلیدی

### راه‌اندازی سرور توسعه
```bash
npm run start:dev
# اجرا با قابلیت Hot-Reload و لاگ‌های رنگی
```

### بیلد برای پروداکشن
```bash
npm run build
# کامپایل کدها به JavaScript خالص
```

### مدیریت دیتابیس
```bash
npx prisma generate  # تولید کلاینت
npx prisma migrate dev  # اعمال تغییرات اسکیما
npx prisma db seed   # پر کردن دیتابیس با داده‌های اولیه
```

### اجرای تست‌ها
```bash
npm test
# اجرای Unit Testها
```

## 📚 مستندات API
پس از اجرای سرور، مستندات کامل Swagger در آدرس زیر در دسترس است:
`http://localhost:8000/api/docs`