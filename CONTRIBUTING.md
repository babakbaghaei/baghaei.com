# راهنمای مشارکت در توسعه (Contributing Guide)

به پروژه گروه فناوری بقایی خوش آمدید. این سند استانداردهای توسعه و فرآیندهای مورد نیاز برای مشارکت در این پروژه را توضیح می‌دهد.

## 🛠 پیش‌نیازها

- **Node.js**: نسخه 20 یا بالاتر.
- **Docker**: برای اجرای دیتابیس لوکال.
- **SSH Key**: برای دیپلوی امن.

## 🚀 شروع به کار

1. **نصب وابستگی‌ها:**
   در روت هر دو پروژه (`backend` و `frontend`) دستور زیر را اجرا کنید:
   ```bash
   npm install
   ```

2. **تنظیم متغیرهای محیطی:**
   از فایل‌های `.env.example` کپی بگیرید و مقادیر را تنظیم کنید:
   ```bash
   cp backend/.env.example backend/.env
   cp frontend/.env.example frontend/.env
   ```

3. **اجرای محیط توسعه:**
   - **Backend:** `cd backend && npm run start:dev`
   - **Frontend:** `cd frontend && npm run dev` (از Turbopack استفاده می‌کند)

## 🧪 استانداردها و کیفیت کد

### Git Hooks (Husky)
ما از Husky برای تضمین کیفیت کد قبل از کامیت استفاده می‌کنیم. اسکریپت `pre-commit` به طور خودکار موارد زیر را چک می‌کند:
- **Linting:** اجرای ESLint روی تمام فایل‌های تغییر یافته.
- **Type Checking:** بررسی صحت تایپ‌های TypeScript.

**توجه:** اگر کدی دارای خطا باشد، کامیت انجام نخواهد شد. لطفاً قبل از کامیت، مشکلات را برطرف کنید.

### ساختار کامیت
لطفاً از فرمت استاندارد برای پیام‌های کامیت استفاده کنید (مثال: `feat: add new project module` یا `fix: resolve hydration error`).

## 🏗 معماری

### Frontend
- **Framework:** Next.js 16 (App Router)
- **Styling:** Tailwind CSS v4
- **Performance:** SSR + next/font + next/image
- **Build Tool:** Turbopack (Dev) / Webpack (Prod)

### Backend
- **Framework:** NestJS
- **ORM:** Prisma (PostgreSQL)
- **Security:** Helmet, Rate Limiting, Input Sanitization
- **Logging:** Pino (JSON in Prod, Pretty in Dev)

## 🚢 دیپلوی
برای دیپلوی روی سرور پروداکشن، از اسکریپت `deploy.sh` در روت پروژه استفاده کنید. مطمئن شوید که کلید SSH شما روی سرور تنظیم شده است.

```bash
./deploy.sh
```

---
با تشکر از مشارکت شما در ساخت آینده‌ای بهتر! 🌟
